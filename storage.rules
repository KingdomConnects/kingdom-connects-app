rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;             // relax for dev
      allow write: if request.auth != null;
    }
  }
}
